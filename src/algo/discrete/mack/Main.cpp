#include "Options.h"

/*
#include "MackAlgo.h"

void setup() {
}

mack::MackAlgo algo;
mack::MouseInterface mouse;
void loop() {
    // Note: solve blocks forever, so loop will only execute once
    algo.solve(mouse);
}
*/
