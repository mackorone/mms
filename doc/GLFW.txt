https://www.opengl.org/wiki/Related_toolkits_and_APIs

"GLFW is a library, written in C, specifically targeted at OpenGL providing the bare necessities required
for rendering goodies to the screen. It allows us to create an OpenGL context, define window parameters
and handle user input which is all that we need." - http://learnopengl.com/#!Getting-started/Creating-a-window

=============

First, install CMAKE. This is a tool that helps us to install other tools.
http://www.cmake.org/download/

(you can just use it in place without removing older versions - just specify the path)

=============

Compiling GLFW

First download it. Then go to the following for instructions on how to complile.
http://www.glfw.org/docs/latest/compile.html

cmake CMakeLists.txt
make
sudo make install

=================

update OpenGL drivers


=================

Everything in OpenGL is done with triangles. and vertexes have to have their own color. That's just the way it goes.

the tgb is layed out in a very particular way

T(0,0)
T(0,1)
...
T(15,15)
mouse

this is so that we can just assign values in the buffer
